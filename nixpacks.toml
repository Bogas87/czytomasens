[phases.setup]
nixPkgs = ["nodejs_24", "npm-11_x"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s dist -l tcp://0.0.0.0:$PORT"
